interface:
  display_name: "Codex Harness Free Model Router"
  short_description: "Configure a user-owned model fallback route"
  default_prompt: "Use $codex-harness-free-model-router to configure and verify this user's model fallback route."

policy:
  allow_implicit_invocation: true
